{
  "version": "1.0",
  "name": "cli",
  "stack": "nodejs",
  "package_manager": "pnpm",
  "build": {
    "command": "pnpm install && pnpm build",
    "output_dir": "dist"
  },
  "runtime": {
    "start_command": "pnpm start",
    "env": {
      "NODE_ENV": "production"
    }
  }
}
